import React from 'react';

import Empty from '@components/Empty/Empty';
import { MainEmpty } from '@components/Empty/MainEmpty';

const empty = () => {
  return (
    <div>
      <MainEmpty />
    </div>
  );
};

export default empty;
